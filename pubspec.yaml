name: tutotial_app
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  google_nav_bar: ^5.0.6
  google_fonts:
  hive_flutter:
  hive: 
  flutter_screenutil:
  path_provider: 
  hive_generator:
  build_runner: 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  

flutter:
  uses-material-design: true
  assets:
    - lib/images/
